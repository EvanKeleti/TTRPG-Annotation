# TTRPG_Annotation
For Brandeis Course COSI 230B - Natural Language Annotation for Machine Learning

## Setting Up the Project
First, set up the project environment with `uv sync`.
Next, initialize the Label Studio project with `make setup`.
In Label Studio, open the project 'TTRPG-Annotations', then click 'Import' and import `data.json`.
